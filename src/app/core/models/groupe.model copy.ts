import { Groupe } from "./groupe.model";

export interface Equipe {
  id: number;
  nom: string;
  couleur: string;

  // createdBy:string;
  // createdAt:string;
  // updatedAt: string;
  // updatedBy: string
}